Prior to running the program, the user must enter one argument:
    1. Directory/JAR file

To run this program using command line, follow the following steps:

    1. Compile CountJavaTypes.java using the javac command
    2. Prior to running the compiled file, the user must enter one command line arguments, being the directory/jar file of interest.
    
NOTE: Remember to set the argument prior to running the program.
