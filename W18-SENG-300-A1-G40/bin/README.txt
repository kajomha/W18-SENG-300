Prior to running the program, the user must enter two arguments;
    1. Directory/JAR file
    2. Java type

To run this program using command line, follow the following steps:

    1. Compile <insertFileName>.java using the javac command
    2. Prior to running the compiled file, the user must enter two command line arguments. The first argument being the directory/jar file of interest, followed by the type the user is searching for.
    
NOTE: Remember to set the arguments prior to running the program, IN THE CORRECT ORDER.
